import Layout from '../components/Layout';

const Contact = () => {
    return (
        <Layout>
            <h1>Contact Me</h1>
            <p>This is the contact page where visitors can reach out to me.</p>
            {/* Add a contact form or contact details here */}
        </Layout>
    );
};

export default Contact;
